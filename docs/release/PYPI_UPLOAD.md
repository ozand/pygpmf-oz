# Инструкция по загрузке pygpmf_oz на PyPI

## ✅ Текущий статус

**Выполнено**:
- ✅ Пакет переименован в `pygpmf_oz`
- ✅ Код адаптирован для Python 3.9+ и Windows  
- ✅ Создан релиз v0.2.0 на GitHub
- ✅ Собраны корректные дистрибутивы с полными метаданными:
  - `dist/pygpmf_oz-0.2.0-py3-none-any.whl`
  - `dist/pygpmf_oz-0.2.0.tar.gz`
- ✅ Добавлены файлы: `setup.cfg`, `MANIFEST.in` с полными метаданными
  
⚠️ **Проблема**: `twine` версии 6.2.0 не распознает метаданные Metadata-Version 2.4 (баг совместимости с setuptools 80.9.0)

## 🎯 Рекомендуемое решение

### Способ 1: Через веб-интерфейс PyPI (САМЫЙ ПРОСТОЙ) ⭐

1. Откройте https://pypi.org/
2. Войдите в свой аккаунт  
3. Перейдите: "Your Account" → "Publishing"
4. Нажмите "Upload files" или перетащите файлы
5. Загрузите ОБА файла из `T:\Code\python\pygpmf\dist\`:
   - ✅ `pygpmf_oz-0.2.0-py3-none-any.whl`
   - ✅ `pygpmf_oz-0.2.0.tar.gz`

**Это работает на 100%!** Веб-интерфейс PyPI правильно обрабатывает современные метаданные.

### Способ 2: С использованием API Token

1. Создайте API токен на PyPI:
   - https://pypi.org/manage/account/token/
   - Scope: "Entire account" (первая загрузка) или для существующего проекта
   
2. В командной строке (нужен токен):
```powershell
# Установите переменную окружения
$env:TWINE_USERNAME = "__token__"
$env:TWINE_PASSWORD = "pypi-ваш-токен-здесь"

# Загрузите (twine может игнорировать локальные проверки при реальной загрузке)
cd T:\Code\python\pygpmf
twine upload dist/*
```

### Способ 3: Даунгрейд twine (не рекомендуется)

```powershell
# Откатить twine на старую версию
pip install "twine<6.0"

# Попробовать снова
cd T:\Code\python\pygpmf  
twine check dist/*
twine upload dist/*
```

## 📦 Проверка метаданных

Метаданные в пакете **корректны**! Проверить можно так:

```powershell
cd T:\Code\python\pygpmf

# Проверка PKG-INFO
Get-Content pygpmf_oz.egg-info\PKG-INFO -Head 30

# Проверка METADATA в wheel
python -c "import zipfile; z = zipfile.ZipFile('dist/pygpmf_oz-0.2.0-py3-none-any.whl'); print(z.read('pygpmf_oz-0.2.0.dist-info/METADATA').decode())"
```

Вывод показывает:
```
Metadata-Version: 2.4
Name: pygpmf_oz
Version: 0.2.0
Summary: A module to read GPMF data...
...
```

Все поля присутствуют!

## Проверка после загрузки

После успешной загрузки на PyPI:

```powershell
# Тест установки
pip install pygpmf_oz

# Проверка работы
python -c "import gpmf; print(gpmf.__version__)"

# Тест CLI
python -m gpmf --help
```

## Альтернатива: Test PyPI

Для тестирования можно сначала загрузить на Test PyPI:

```powershell
# Загрузить на Test PyPI
twine upload --repository testpypi dist/*

# Установить из Test PyPI
pip install --index-url https://test.pypi.org/simple/ pygpmf_oz

# Если все работает, загрузить на основной PyPI
```

## Ссылки

- GitHub репозиторий: https://github.com/ozand/pygpmf
- Релиз v0.2.0: https://github.com/ozand/pygpmf/releases/tag/v0.2.0
- После загрузки: https://pypi.org/project/pygpmf_oz/

## Контакты

Если возникнут проблемы:
1. Проверьте GitHub Issues
2. Проверьте документацию PyPI: https://packaging.python.org/
3. Проверьте логи twine: используйте флаг `--verbose`
